<h1>Lists#index</h1>

<% @lists.each do |list| %>
  <%= link_to "#{list.name}", list_path(list), class: "btn btn-success" %><br>
<% end %><br><br>

<%= link_to "Créer une liste de film", new_list_path, class: "btn btn-primary" %>
